module.exports = {
    presets: [
      '@babel/preset-env',
      '@babel/preset-react',
      '@babel/preset-typescript'
    ],
    plugins: [
      // Plugins for handling CSS-in-JS or CSS modules
      // Example:
    ],
  }